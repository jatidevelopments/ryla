# Track images with LFS by default
*.webp filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text
*.jpeg filter=lfs diff=lfs merge=lfs -text

# Exclude funnel images from LFS (small files, needed for static export)
apps/funnel/public/images/**/*.webp filter= diff= merge=
apps/funnel/public/images/**/*.png filter= diff= merge=
apps/funnel/public/images/**/*.jpg filter= diff= merge=
apps/funnel/public/images/**/*.jpeg filter= diff= merge=
